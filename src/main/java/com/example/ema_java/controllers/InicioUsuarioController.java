package com.example.ema_java.controllers;

import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Scene;
import javafx.scene.control.PasswordField;
import javafx.scene.control.TextField;
import javafx.stage.Stage;

import java.util.Objects;

public class InicioUsuarioController {
    @FXML
    private TextField tField;
    @FXML
    private PasswordField pwField;

    @FXML
    protected void onHelloButtonClick() {
//        if (Objects.equals(tField.getText(), "hola") && Objects.equals(pwField.getText(), "quihubo")) {
//            Stage inicio = new Stage();
//            FXMLLoader fxmlLoader = new FXMLLoader(HelloApplication.class.getResource("inicio-usuario-view.fxml"));
//            Scene scene = new Scene(fxmlLoader.load(), 200, 500);
//            inicio.setScene(scene);
//        }
    }
}